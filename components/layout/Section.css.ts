import { style } from '@vanilla-extract/css'

export const base = style({
  display: 'flex',
  flexDirection: 'column',
  maxWidth: '42.5rem',
  minWidth: 0,
  flex: 1,
  margin: 0,
  overflow: 'hidden',
})
